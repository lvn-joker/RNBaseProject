import React from 'react';
import SplashScreen from 'app/src/screen/home/index';
import { Provider } from 'react-redux';
import NavigateContainer from 'app/src/navigation/container';

const AppContainer = () => {
    return (
        <NavigateContainer/>
    )
};

export default AppContainer;
